describe('search query validation', () => {
    test("should not search if the query is empty", () => {

    });

    test("Should not search if there are no letters or numbers in the v", () => {

    });
});

describe('add name', () => {

    test("should return one name and one group with a capital letter after adding", () => {

    });

    test("should return needle after adding", () => {

    });

});

describe('search and add name', () => {

    test("should return page 'no-result' if groups are empty", () => {

    });

});