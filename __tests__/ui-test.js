test("should not search if the request is empty", () => {
      
});

test("should not page 'no-result' if groups is empty", () => {
      
});

test("should return one name with a capital letter after adding", () => {
      
});

test("should return one group with a capital letter after adding", () => {
      
});