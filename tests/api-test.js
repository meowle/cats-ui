
describe("searchPattern", () => {
    test("should not search if the request is empty", () => {
      
    });
    test("should search if the query contains 1 character", () => {
      
    });
    test("should search if there are many characters in the request", () => {
      
    });
    test("should search if the query contains the numbers", () => {
      
    });
    test("should search if there are special symbol in the request", () => {
      
    });
    test("should search if the request contains Latin and Cyrillic", () => {
      
    });
    test("should search if only Latin is present in the request", () => {
      
    });  
    test("should search if there are 2 words in the request in a space", () => {
      
    });
    test("should search if there are 2 words in the request through the special symbol", () => {
      
    });
    test("should search if there are 5 words in the request", () => {
      
    });  
  });